Write a Java method to return the cubed value of some number.
public int cube(int a) {
	return a*a*a;
}

Write an Arduino function to return the cubed value of some number.
int cube(int a) {
	return a*a*a;
}

Create the class, an object, and class constructor for class student
public class Student {
	int gpa;
	int tiredness;
	public Student(int a, int b) {
		this.gpa = a;
		this.tiredness = b;	
	}
}

Student meeee;
meeee = new Student(106, 60);

Describe - if we were to continue with Arduino - what would you create?
I would create something without buttons. I might use the pot to change the hue of an rgb, or the frequency and the lighting outputs of a series of
lights indicating this frequency.
